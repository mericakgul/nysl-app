import React from 'react';

//TODO
const UploadPictureForm = ({gameId, user}) => {
    return (
        <form>
            <button type="button">Upload Picture</button>
        </form>
    );
};

export default UploadPictureForm;